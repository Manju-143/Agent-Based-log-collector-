# Core data modelling
pydantic>=2.5

# Cryptography and PKI
cryptography>=42.0

# Noise Protocol secure transport
noiseprotocol>=0.3.1

# Async HTTP (optional but useful later)
aiohttp>=3.9

# OpenSearch client
opensearch-py>=2.5

# Environment configuration (optional but good practice)
python-dotenv>=1.0

# For future Kubernetes / Docker health endpoints
uvicorn>=0.27
fastapi>=0.109